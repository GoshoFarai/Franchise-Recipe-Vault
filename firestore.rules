match /users/{userId} {
  allow create: if request.auth != null && request.auth.uid == userId;
  allow read, update: if request.auth != null && (resource.status == 'approved' || request.auth.token.admin == true);
}